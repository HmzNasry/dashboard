node_modules
dist
media/generated
.venv-piper
devices.json
*.log
.DS_Store

# Voices ARE committed (so a fresh clone works offline). The Piper engine
# (.venv-piper / piper binary) is recreated by scripts/setup-tts.mjs on start.
